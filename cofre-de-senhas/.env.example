# URL do backend (server/). Em desenvolvimento local, o padrão já funciona.
# Em produção, aponte para onde o backend estiver hospedado (Railway, Oracle
# Cloud, etc.) — por exemplo: https://cofre-de-senhas-api.up.railway.app
VITE_API_BASE_URL=http://localhost:3001
